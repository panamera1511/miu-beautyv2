import styles from "./SearchInput.module.scss";
import classNames from "classnames/bind";
const cx = classNames.bind(styles);

export default function FormPost() {
    <div className={cx("post_title")}>
        <h1>Đăng đơn hàng</h1>
    </div>
}